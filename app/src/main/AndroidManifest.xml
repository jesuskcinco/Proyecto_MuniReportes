<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoandroid">

    <application
        android:name=".Utilidades.GlobalVariables"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReporteVehiculos"></activity>
        <activity android:name=".RegistrarMotoSociedad" />
        <activity android:name=".AceptarConductor" />
        <activity android:name=".BandejaMensajes" />
        <activity android:name=".ListaRecyclerBandeja" />
        <activity android:name=".MotoSociedad" />
        <activity android:name=".RegistrarAsociacion" />
        <activity android:name=".RegistroMotoConductor" />
        <activity android:name=".ListaRecyclerMotos" />
        <activity android:name=".ConsultarTodoMototaxis" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".DetalleIncidente" />
        <activity android:name=".ListarRecycler" />
        <activity android:name=".PanelIncidentes" />
        <activity android:name=".RegistrarIncidente" />
        <activity android:name=".RegistrarNivelncidente" />
        <activity android:name=".RegistrarTipoIncidente" />
        <activity android:name=".ActualizarMototaxi" />
        <activity android:name=".Consultar_Mototaxis" />
        <activity android:name=".RegistrarMototaxi" />
        <activity android:name=".IniciarSesion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActualizarUsuario" />
        <activity android:name=".EliminarUsuario" />
        <activity android:name=".BuscarUsuario" />
        <activity android:name=".RegistrarUsuario" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <uses-feature android:name="android.hardware.location.gps" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>